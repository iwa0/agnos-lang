const GLOBAL = x! + 5
y = 123

struct Stru<T, N: u32>
    Ss: S
    Sarr: array!<T,N>
    Sa: T

enum Enu
    Ea
    Eb
    Ec

union Uni
    Unia: A

func fun<T>(x1: T, y2: &T, z3: &&T) u64
    defer abort()
    let avar = await b c for d::e
    let test = yield x
    return .asd{123, .asd(123), .a}


func f(x1: u32) u8
    return 5

func* f(x1: S) xd
    return .a(1)

let xlet = await a(123,4,5).xd() for 1
var .avar {{.., =1, =5, _}, test}: .fn!<u32, u64, .unit> = a!<a a>::a!::a d
let v