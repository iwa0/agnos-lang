const z = x! + 5
y = 123

struct S<T, N: u32>
    s: S
    arr: array<T,N>
    a: T

enum E
    a
    b
    c

union U
    a: A

func f<T>(x: T, y: &T, z: &&T) u64
    let a = await b c for d::e
    let test = yield x
    return .asd{123, .asd(123), .a}


func f(u: u32) u8
    return 5

func *f(s: S) xd
    return .a(1)

let x = await a(123,4,5).xd() for 1
var .a {{.., =1, =5, _}, test}: .fn<u32, u64, .unit> = a!<a a>::a!::a d
let v